apiVersion: v1
kind: Service
metadata:
  name: {{ include "jasoseoai_nextjs.fullname" . }}
  labels:
    {{- include "jasoseoai_nextjs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jasoseoai_nextjs.selectorLabels" . | nindent 4 }}
