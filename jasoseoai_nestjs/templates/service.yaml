apiVersion: v1
kind: Service
metadata:
  name: {{ include "jasoseoai_nestjs.fullname" . }}
  labels:
    {{- include "jasoseoai_nestjs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jasoseoai_nestjs.selectorLabels" . | nindent 4 }}
